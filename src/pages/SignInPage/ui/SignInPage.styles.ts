import type { SxProps } from '@mui/material'

const page: SxProps = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100%',
}

const formContainer: SxProps = {
  width: '500px',
  mx: 'auto',
  mb: 20,
}

export default { formContainer, page }
