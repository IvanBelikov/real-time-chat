import type { SxProps } from '@mui/material'

const buttonContent: SxProps = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  gap: '4px',
}

export default { buttonContent }
